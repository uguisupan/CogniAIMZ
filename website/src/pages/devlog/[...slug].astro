---
import Layout from '../../layouts/Layout.astro';
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
    const posts = await getCollection('devlog');
    return posts.map(post => ({
        params: { slug: post.id },
        props: { post },
    }));
}

const { post } = Astro.props;
const { Content } = await render(post);

// 日付フォーマット
const formatDate = (date: Date) => {
    const y = date.getFullYear();
    const m = String(date.getMonth() + 1).padStart(2, '0');
    const d = String(date.getDate()).padStart(2, '0');
    return `${y}.${m}.${d}`;
};
---

<Layout title={post.data.title} hideNav={true}>
    <article class="product-card" style="min-height: auto; width: 100%; padding: 40px; cursor: default; margin-bottom: 20px;">
        <header style="margin-bottom: 30px; border-bottom: 2px solid var(--color-navy); padding-bottom: 15px;">
            <div class="blog-meta" style="margin-bottom: 10px; font-size: 0.85rem;">
                <span class="blog-date font-mono" style="color: var(--color-cyan); font-weight: bold;">{formatDate(post.data.pubDate)}</span>
                <span class="blog-tag font-mono" style="color: var(--color-pink); margin-left: 20px; font-weight: bold;">#{post.data.tag}</span>
            </div>
            <h1 style="font-size: 1.6rem; font-weight: bold; color: var(--color-navy); line-height: 1.4; text-shadow: 1px 1px 0px rgba(0,0,0,0.05);">
                {post.data.title}
            </h1>
        </header>

        <div class="markdown-content">
            <Content />
        </div>

        <footer style="margin-top: 40px; border-top: 1px dashed rgba(0,0,0,0.1); padding-top: 20px;">
            <div class="back-navigation" style="display: flex; flex-direction: column; gap: 8px;">
                <div>
                    <a href="https://cogniaimz.chikyu.in/" class="product-action-btn font-mono" style="text-decoration: none; padding: 6px 18px; font-size: 0.85rem; display: inline-block; border-color: var(--color-pink);">
                        ≪ BACK TO OFFICIAL SITE (本家トップ)
                    </a>
                </div>
                <div>
                    <a href={`${import.meta.env.BASE_URL}/`} style="color: var(--color-cyan); text-decoration: underline; font-size: 0.8rem; font-weight: bold; margin-left: 5px;">
                        ➔ 開発ブログ・資料室インデックスへ戻る
                    </a>
                </div>
            </div>
        </footer>
    </article>
</Layout>
